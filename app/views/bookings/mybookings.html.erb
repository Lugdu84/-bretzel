<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" type="text/css" href="style.css">
  <link rel=“stylesheet” href=“https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css” integrity=“sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u” crossorigin=“anonymous”>
</head>

<body>
  <%= render 'shared/search' %>
  <h3>Mes vélos:</h3>
 <!--  <br>
  <a class="btn btn-page4" href="#">Précédents</a>
  <a class="btn btn-page4" href="#">A venir</a>
  <a class="btn btn-page4" href="#">Mes envies</a>
  <br>
  <br> -->
<nav>
  <% @bookings.each do |booking|  %>
    <div class="card-trip-order">
          <%= image_tag booking.bike.photo %>
          <div class="card-trip-order-infos">
            <div>
              <h2><%= booking.bike.name %></h2>
              <p><%= booking.bike.description %></p>
            </div>
            <br>
            <!-- <a class="btn btn-validate" href="#">Envoyer</a> -->
          </div>
          <%= render 'shared/ratingstars' %>
        </div>
  <% end %>
</nav>
</body>
